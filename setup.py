import os


def setup_project() -> None:
    """
    Основная функция установки проекта

    Выполняет следующие действия:
    1. Устанавливает зависимости из requirements.txt
    2. Создает необходимые директории (sessions, log)
    3. Выводит статус установки

    :return: None
    """
    # Установка зависимостей
    print("📦 Устанавливаем зависимости...")
    os.system("pip install -r requirements.txt")

    # Создание необходимых директорий
    create_directory("sessions")
    create_directory("log")

    print("✅ Установка завершена!")


def create_directory(dir_name: str) -> None:
    """
    Создает директорию, если она не существует

    :param dir_name: Имя создаваемой директории
    :return: None
    """
    if not os.path.exists(dir_name):
        os.makedirs(dir_name)
        print(f"📁 Создана директория: {dir_name}")
    else:
        print(f"📁 Директория {dir_name} уже существует")


if __name__ == "__main__":
    setup_project()
